/**
 * コンポーネントのオブジェクト
 * @typedef Component
 * @property {string} insertId コンポーネントが入る要素の id
 * @property {string[]} templates 要素の指定
 * @property {Object.<string, (string | number | Object<string, any>)>} styles スタイルの指定 複数のパラメータを指定することが可能
 * @property {() => void} script スクリプトの指定
 */
